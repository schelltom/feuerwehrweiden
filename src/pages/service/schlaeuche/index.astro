---
import Basis from '../../../layouts/Basis.astro';
import Seitenkopf from '../../../components/Seitenkopf.astro';
import { ladeSchlaeuche, datum } from '../../../lib/pflege';

const vorgaenge = (await ladeSchlaeuche()).filter((v) => !v.abgeholt);
const abholbereit = vorgaenge.filter((v) => v.abholbereit);
---

<Basis
  titel="Schlauchpflegestelle – Feuerwehr Weiden"
  beschreibung="Aktueller Status der Schlauchpflegestelle: Welche Schläuche sind geprüft und abholbereit?"
>
  <main>
    <Seitenkopf krumen={[{ name: 'Start', href: '/' }, { name: 'Service', href: '/service/' }, { name: 'Schlauchpflegestelle' }]} />

    <section>
      <div class="wrap" style="padding-top:clamp(44px,6vw,72px)">
        <p class="intro reveal" style="margin-bottom:36px">
          Die zentrale Schlauchpflegestelle in der Hauptfeuerwache reinigt, prüft und pflegt
          die Feuerwehrschläuche der Wehren der Umgebung. Hier seht ihr, welche Anlieferungen
          geprüft und abholbereit sind.
        </p>

        <div class="statusblock reveal">
          <h2 class="display statuskopf bereit">
            <span class="punkt" aria-hidden="true"></span>
            Abholbereit <em>({abholbereit.length})</em>
          </h2>
          {
            abholbereit.length === 0 ? (
              <p class="leer">Aktuell ist nichts abholbereit.</p>
            ) : (
              <ul class="vorgaenge">
                {abholbereit.map((v) => (
                  <li class="vorgang bereit">
                    <div>
                      <b>{v.wehrName}</b>
                      <span>Schläuche</span>
                      {v.hinweis && <em class="hinweis">⚠ {v.hinweis}</em>}
                    </div>
                    <time>seit {datum(v.abgegeben)}</time>
                  </li>
                ))}
              </ul>
            )
          }
        </div>

        <p style="margin:28px 0">
          <a href="/feuerwehr/ueber-uns/schlauchpflegestelle/" style="font-family:var(--mono);font-size:.8rem;letter-spacing:.12em;color:var(--tinte2);text-decoration:none;border-bottom:1px solid var(--rot)">Was ist die Schlauchpflegestelle? →</a>
        </p>

        <div class="hinweisbox">
          Anlieferung und Abholung zu den Öffnungszeiten der Hauptfeuerwache,
          Landgerichtsstraße 13 · Fragen: <a href="tel:+4996139160911">0961 / 391 609-11</a>
        </div>
      </div>
    </section>
  </main>
</Basis>

<style>
  .statusblock {
    margin-bottom: 44px;
  }
  .statuskopf {
    display: flex;
    align-items: center;
    gap: 12px;
    font-style: italic;
    text-transform: uppercase;
    font-size: clamp(1.4rem, 3vw, 1.9rem);
    margin: 0 0 16px;
  }
  .statuskopf em {
    font-style: italic;
    color: var(--tinte2);
    font-size: 0.7em;
  }
  .statuskopf .punkt {
    width: 12px;
    height: 12px;
    border-radius: 50%;
    flex: none;
  }
  .statuskopf.bereit .punkt {
    background: #1a9b4e;
    box-shadow: 0 0 10px rgba(26, 155, 78, 0.7);
  }
  .vorgaenge {
    list-style: none;
    margin: 0;
    padding: 0;
    display: grid;
    gap: 10px;
  }
  .vorgang {
    display: flex;
    justify-content: space-between;
    align-items: baseline;
    gap: 16px;
    flex-wrap: wrap;
    background: var(--karte);
    border: 1px solid var(--linie);
    border-left: 4px solid var(--linie);
    padding: 14px 18px;
    box-shadow: 0 2px 10px rgba(20, 22, 28, 0.04);
  }
  .vorgang.bereit {
    border-left-color: #1a9b4e;
  }
  .vorgang b {
    font-family: var(--display);
    font-style: italic;
    text-transform: uppercase;
    font-size: 1.1rem;
    margin-right: 12px;
  }
  .vorgang span {
    color: var(--tinte2);
    font-size: 0.95rem;
  }
  .vorgang .hinweis {
    display: block;
    font-style: normal;
    font-size: 0.85rem;
    color: #b25f00;
    margin-top: 4px;
  }
  .vorgang time {
    font-family: var(--mono);
    font-size: 0.72rem;
    letter-spacing: 0.08em;
    color: var(--tinte2);
    white-space: nowrap;
  }
  .leer {
    color: var(--tinte2);
    font-size: 0.95rem;
    margin: 0;
  }
  .hinweisbox a {
    color: var(--rot);
  }
</style>
